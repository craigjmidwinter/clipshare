# Clipshare Docker Environment Configuration
# Copy this file to .env and configure for your deployment

# Application Environment
NODE_ENV=production
PORT=3000
NEXT_TELEMETRY_DISABLED=1

# Next.js Authentication (REQUIRED for security)
# Generate a secure secret: openssl rand -base64 32
NEXTAUTH_SECRET=your-super-secret-key-at-least-32-characters-long
NEXTAUTH_URL=http://localhost:3000

# Database Configuration (optional - uses default if not set)
# For SQLite (default) - within single data directory:
DATABASE_URL=file:./data/db/dev.db

# File Storage Paths (optional - uses defaults if not set)
# All within single data directory for easy management
CLIPS_DIR=/app/data/processed-files/clips
EXPORT_DIR=/app/data/processed-files/exports
TEMP_DIR=/app/data/temp

# Performance Configuration (optional)
PROCESS_CONCURRENCY=4
MAX_CONCURRENT_JOBS=2
MAX_CLIP_SIZE=500MB

# ========================================
# PLEX CONFIGURATION - HANDLED BY WIZARD
# ========================================
# The following Plex settings are configured through the welcome wizard
# on first startup and stored in the database. You don't need to set 
# these as environment variables:
#
# - PLEX_CLIENT_ID (hardcoded in app)
# - PLEX_SERVER_URL (set via wizard)  
# - PLEX_SERVER_TOKEN (set via wizard)
#
# Just start the app and the first user will be guided through setup!